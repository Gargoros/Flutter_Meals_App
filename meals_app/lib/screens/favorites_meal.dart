import 'package:flutter/material.dart';

class FavoritesMealScreen extends StatelessWidget {
  const FavoritesMealScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("The Favorites Meal"),
    );
  }
}
